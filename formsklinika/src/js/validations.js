/* export function validacionEmail(valor) {
	let validacionEmail = false;
	const emailRegex = /^\\S+@\\S+\\.\\S+$/;
	
	if (emailRegex.test(valor)) {
		console.log("Email correcto.");
		validacionEmail = true;
	} else {
		console.log("Email no tiene el formato correcto @ .");
		validacionEmail = false;
	}
}

export function validacionPassword(valor) {
	let validacionPassword = false;
	const passwordRegex = /^[0-9a-zA-Z]$/i;

	if (passwordRegex.test(valor)) {
		console.log("Contraseña aceptada.");
		validacionPassword = true;
	} else {
		console.log(
			"Contraseña debe tener min. 6 carácteres,números, especial, minúscula, mayúscula."
		);
		validacionPassword= false;
	}
}

export function validaciones(valor1, valor2) {

	if (validacionEmail === true && validacionPassword === true) {
		setValidar(false);
		setVisibilidad(true);
	} else if (validacionEmail === true || validacionPassword === true) {
		setValidar(false);
		setVisibilidad(true);
	} else {
		setValidar(true);
		setVisibilidad(false);
	}
}


export function validacionEmail(valor) {
	let validacionEmail = false;
	const emailRegex = /^\\S+@\\S+\\.\\S+$/;
	
	if (emailRegex.test(valor)) {
		console.log("Email correcto.");
		validacionEmail = true;
	} else {
		console.log("Email no tiene el formato correcto @ .");
		validacionEmail = false;
	}
}

export function validacionPassword(valor) {
	let validacionPassword = false;
	const passwordRegex = /^[0-9a-zA-Z]$/i;

	if (passwordRegex.test(valor)) {
		console.log("Contraseña aceptada.");
		validacionPassword = true;
	} else {
		console.log(
			"Contraseña debe tener min. 6 carácteres,números, especial, minúscula, mayúscula."
		);
		validacionPassword= false;
	}
}

export function validaciones(valor1, valor2) {

	if (validacionEmail === true && validacionPassword === true) {
		setValidar(false);
		setVisibilidad(true);
	} else if (validacionEmail === true || validacionPassword === true) {
		setValidar(false);
		setVisibilidad(true);
	} else {
		setValidar(true);
		setVisibilidad(false);
	}
} */


/* Ka1234567#
        carito @gmail.com */